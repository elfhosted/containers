From 56a86e6eed825e295bf082938db611b8f68e9a64 Mon Sep 17 00:00:00 2001
From: David Young <davidy@funkypenguin.co.nz>
Date: Sat, 26 Sep 2026 15:57:51 +1200
Subject: [PATCH] feat: make health-index use follow contribution, under a
 shared name

The index is built from what its clients contribute, so reading it and
writing to it are one decision. That was already half true here: the gated
indexer is refused unless BUILTIN_NAB_HEALTH_CONTRIBUTE is on. Two other
paths were not.

fetchZyclopsHealth now returns no verdicts when not participating.

buildZyclopsHealthProxyConfig does too, and that one is the reason this is
worth care. Routing a search through the health proxy makes no health API
call at all -- the search URL is simply rewritten to the proxy, and every
result still comes from the index. Gating only the queries missed it
completely, and a live search on a real configuration proved it: with
participation off, requests still went to the proxy endpoint. The user's own
zyclopsHealthProxy flag is their preference; participation is whether it may
be honoured.

Adds ZYCLOPS_INGEST (default true) as a second way to say the same thing.
Either setting being false opts out.

Note for existing deployments: an instance that sets a health API URL, or
enables the health proxy on a preset, without turning contribution on will
find both stop working. That is the intended reciprocity, and the gated
indexer has always been refused under exactly that condition.

The tests carry a list of every consumer of the rule. It is a checklist for
review rather than a behavioural assertion -- the rule is shared, the call
sites are not, and a missed call site is invisible to a test of the rule.
That is precisely how the proxy-routing path was missed on the first cut.
---
 packages/core/src/builtins/newznab/addon.ts   | 15 ++++++
 .../src/builtins/utils/nzbContribution.ts     |  4 +-
 .../src/builtins/utils/sharedIndex.test.ts    | 46 +++++++++++++++++++
 .../core/src/builtins/utils/sharedIndex.ts    | 38 +++++++++++++++
 .../core/src/builtins/utils/zyclopsHealth.ts  |  4 ++
 packages/core/src/config/schema/builtins.ts   | 10 ++++
 packages/core/src/presets/newznab.ts          |  5 +-
 7 files changed, 119 insertions(+), 3 deletions(-)
 create mode 100644 packages/core/src/builtins/utils/sharedIndex.test.ts
 create mode 100644 packages/core/src/builtins/utils/sharedIndex.ts

diff --git a/packages/core/src/builtins/newznab/addon.ts b/packages/core/src/builtins/newznab/addon.ts
index dfbc3ef..40c3c68 100644
--- a/packages/core/src/builtins/newznab/addon.ts
+++ b/packages/core/src/builtins/newznab/addon.ts
@@ -31,6 +31,7 @@ import {
 import type { BuiltinServiceId } from '../../utils/index.js';
 import type { Stream } from '../../db/index.js';
 import { isBypassedHost, parseBypassHosts } from '../utils/replayBypass.js';
+import { isOptedOut } from '../utils/sharedIndex.js';
 
 const logger = createLogger('newznab');
 
@@ -149,6 +150,20 @@ export class NewznabAddon extends BaseNabAddon<NewznabAddonConfig, NewznabApi> {
       return undefined;
     }
 
+    // Routing a search through the health proxy IS reading the index, so it
+    // follows participation like the queries and the gated indexer do. The
+    // user's own flag is their preference; this is whether it may be honoured.
+    //
+    // Gating only fetchZyclopsHealth would miss this entirely: no health API
+    // call is made on this path, the search URL is simply rewritten to the
+    // proxy, and every result still comes from the index.
+    if (isOptedOut()) {
+      logger.info(
+        'Not routing this search through the health proxy: this instance is not participating in the shared health index.'
+      );
+      return undefined;
+    }
+
     const endpoint = appConfig.builtins.nab.zyclopsHealthProxyEndpoint;
     const path = '/api';
     const extraParams: Record<string, string | number | boolean> = {};
diff --git a/packages/core/src/builtins/utils/nzbContribution.ts b/packages/core/src/builtins/utils/nzbContribution.ts
index 0278e43..73484d8 100644
--- a/packages/core/src/builtins/utils/nzbContribution.ts
+++ b/packages/core/src/builtins/utils/nzbContribution.ts
@@ -3,6 +3,7 @@ import crypto from 'crypto';
 import { createLogger } from '../../utils/index.js';
 import { makeUrlLogSafe } from '../../utils/http.js';
 import { config as appConfig } from '../../config/index.js';
+import { isOptedOut } from './sharedIndex.js';
 
 const logger = createLogger('nzb-contribution');
 
@@ -32,7 +33,8 @@ export function contributeNzb(
   source: string,
   name?: string
 ): void {
-  if (!appConfig.builtins.nab.healthContribute) return;
+  // Contributing and querying are one decision -- see sharedIndex.
+  if (isOptedOut()) return;
   const url = appConfig.builtins.nab.healthIngestUrl;
   if (!url) {
     logger.debug('NZB contribution enabled but no ingest endpoint is set.');
diff --git a/packages/core/src/builtins/utils/sharedIndex.test.ts b/packages/core/src/builtins/utils/sharedIndex.test.ts
new file mode 100644
index 0000000..b08a1de
--- /dev/null
+++ b/packages/core/src/builtins/utils/sharedIndex.test.ts
@@ -0,0 +1,46 @@
+import { describe, it } from 'node:test';
+import assert from 'node:assert/strict';
+import { optedOutFor } from './sharedIndex.js';
+
+// Every consequence of opting out keys off this one rule. Naming them here so
+// a later change that adds a fourth consumer has something to notice: the
+// rule is shared, the call sites are not, and a missed call site is invisible
+// to these tests.
+const CONSUMERS = [
+  'contributeNzb -- no upload',
+  'fetchZyclopsHealth -- no queries',
+  'the gated indexer -- refused',
+  'buildZyclopsHealthProxyConfig -- searches not routed through the proxy',
+];
+
+describe('shared index participation', () => {
+  it('participates only when both settings agree', () => {
+    assert.equal(optedOutFor(true, true), false);
+  });
+
+  it('opts out on either setting', () => {
+    // Reading and contributing are one decision, so neither survives alone.
+    assert.equal(optedOutFor(false, true), true);
+    assert.equal(optedOutFor(true, false), true);
+    assert.equal(optedOutFor(false, false), true);
+  });
+
+  it('treats an unset contribute setting as not participating', () => {
+    // It defaults to false, and the gated indexer has always been refused on
+    // that basis -- querying now follows the same rule.
+    assert.equal(optedOutFor(true, undefined), true);
+  });
+
+  it('covers every consumer of the rule', () => {
+    // Not a behavioural assertion: a checklist that fails review, not CI, if
+    // a consumer is added without being gated. The proxy-routing one was
+    // missed on the first cut and only surfaced by driving a real search.
+    assert.equal(CONSUMERS.length, 4);
+  });
+
+  it('treats an unset participation setting as participating', () => {
+    // Its default is true, so only an explicit false opts out through it.
+    assert.equal(optedOutFor(undefined, true), false);
+    assert.equal(optedOutFor(undefined, false), true);
+  });
+});
diff --git a/packages/core/src/builtins/utils/sharedIndex.ts b/packages/core/src/builtins/utils/sharedIndex.ts
new file mode 100644
index 0000000..088932c
--- /dev/null
+++ b/packages/core/src/builtins/utils/sharedIndex.ts
@@ -0,0 +1,38 @@
+import { config as appConfig } from '../../config/index.js';
+
+/**
+ * Participation in the shared health index.
+ *
+ * The index is built from the NZBs its participants contribute, so reading it
+ * and writing to it are one decision, not two. An instance that is not
+ * contributing does not query the index and cannot use the gated indexer.
+ * There is deliberately no "read but do not write" mode.
+ *
+ * Two settings say the same thing:
+ *
+ *   ZYCLOPS_INGEST                  false opts out. This is the name shared
+ *                                   with the other clients of the index, so
+ *                                   one documented setting means the same
+ *                                   thing wherever it is set.
+ *   BUILTIN_NAB_HEALTH_CONTRIBUTE   false also opts out. It predates the
+ *                                   shared name and already governed both
+ *                                   contribution and the gated indexer.
+ *
+ * Either being false is enough.
+ */
+/**
+ * The rule itself, as a function of the two settings. Kept separate from the
+ * settings read so it can be exercised directly: appConfig is not resolved at
+ * module-load time, so a test cannot read it.
+ */
+export const optedOutFor = (
+  zyclopsIngest: boolean | undefined,
+  healthContribute: boolean | undefined
+): boolean => zyclopsIngest === false || healthContribute !== true;
+
+/** Binds the rule to the resolved settings. */
+export const isOptedOut = (): boolean =>
+  optedOutFor(
+    appConfig.builtins.nab.zyclopsIngest,
+    appConfig.builtins.nab.healthContribute
+  );
diff --git a/packages/core/src/builtins/utils/zyclopsHealth.ts b/packages/core/src/builtins/utils/zyclopsHealth.ts
index 93f2823..9a096a2 100644
--- a/packages/core/src/builtins/utils/zyclopsHealth.ts
+++ b/packages/core/src/builtins/utils/zyclopsHealth.ts
@@ -1,6 +1,7 @@
 import { fetch } from 'undici';
 import { createLogger } from '../../utils/index.js';
 import { config as appConfig } from '../../config/index.js';
+import { isOptedOut } from './sharedIndex.js';
 
 const logger = createLogger('zyclops-health');
 
@@ -102,6 +103,9 @@ export async function fetchZyclopsHealth(
   );
   if (!descriptors.length || !hosts.length) return result;
 
+  // Querying the index is a benefit of contributing to it.
+  if (isOptedOut()) return result;
+
   const url = appConfig.builtins.nab.zyclopsHealthApiUrl;
   if (!url) return result;
 
diff --git a/packages/core/src/config/schema/builtins.ts b/packages/core/src/config/schema/builtins.ts
index 5f4ad7f..fccf4eb 100644
--- a/packages/core/src/config/schema/builtins.ts
+++ b/packages/core/src/config/schema/builtins.ts
@@ -669,6 +669,16 @@ export const builtinsSchema = {
       requiresRestart: false,
       secret: false,
     },
+    zyclopsIngest: {
+      schema: z.boolean(),
+      default: true,
+      label: 'Participate in the shared health index',
+      env: 'ZYCLOPS_INGEST',
+      description:
+        'When false, this instance neither contributes to nor queries the shared health index, and cannot use the gated indexer. Reading and contributing are one setting.',
+      requiresRestart: false,
+      secret: false,
+    },
     healthContribute: {
       schema: z.boolean(),
       default: false,
diff --git a/packages/core/src/presets/newznab.ts b/packages/core/src/presets/newznab.ts
index 2519ef7..ff2b4a1 100644
--- a/packages/core/src/presets/newznab.ts
+++ b/packages/core/src/presets/newznab.ts
@@ -2,6 +2,7 @@ import { Addon, Option, ParsedStream, Stream, UserData } from '../db/index.js';
 import { Preset, baseOptions } from './preset.js';
 import { appConfig, RESOURCES, ServiceId, constants } from '../utils/index.js';
 import { BuiltinAddonPreset, BuiltinStreamParser } from './builtin.js';
+import { isOptedOut } from '../builtins/utils/sharedIndex.js';
 
 class NewznabStreamParser extends BuiltinStreamParser {
   protected override getMessage(
@@ -379,11 +380,11 @@ export class NewznabPreset extends BuiltinAddonPreset {
     options: Record<string, any>
   ): Promise<Addon[]> {
     const gatedIndexer = appConfig.builtins.nab.healthIndexerUrl;
-    if (gatedIndexer && !appConfig.builtins.nab.healthContribute) {
+    if (gatedIndexer && isOptedOut()) {
       const configured = options.api?.url;
       if (configured && sameEndpoint(configured, gatedIndexer)) {
         throw new Error(
-          `${this.METADATA.NAME}: this indexer requires NZB contribution to be enabled (BUILTIN_NAB_HEALTH_CONTRIBUTE).`
+          `${this.METADATA.NAME}: this indexer requires participation in the shared health index (ZYCLOPS_INGEST / BUILTIN_NAB_HEALTH_CONTRIBUTE).`
         );
       }
     }
