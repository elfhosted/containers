#!/bin/bash
echo "It's not possible to use [cd] to change directories in Filebrowser"
echo "Instead, navigate the file structure using the GUI elements above, and the $PWD in the"
echo "console will change as you do :)"